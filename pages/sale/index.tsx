import React from 'react';
import Layout from '../../components/Layout';

const Sale = () => {
  return (
    <Layout
      seo={{
        title: 'Sale | QuackStore',
        description: 'Sale Stock | QuackStore',
        canonicalUrl: 'https://quackstore.com/sale',
      }}
    >
      Sale
    </Layout>
  );
};

export default Sale;
